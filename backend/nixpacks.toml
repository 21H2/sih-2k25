[phases.setup]
nixPkgs = ['python311', 'pip']

[phases.install]
cmds = ['pip install -r requirements.txt']

[phases.build]
cmds = ['python extract_model.py']

[start]
cmd = 'gunicorn whatsapp_bot:app --bind 0.0.0.0:$PORT'